- **Fixed docs: leftover pre-ADR-006 teaching is gone.** Collection `equal?`
  is content equality, hashes are one string keyspace rather than
  symbol-keyed, the `function` type is not listed as live, and the 1.0
  migration guide now covers Tasks/`sleep` and the string keyspace. Hover on
  `Proc` and `include`/`extend` teaches the removals.
