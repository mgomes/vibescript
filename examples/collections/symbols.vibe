# vibe: 1.0

def permission_keys
  [:read, :write, :delete]
end

def fetch_hash_key(record, key)
  record[key]
end

def normalized_hash_view(record)
  {
    same_entry: record[:name] == record["name"],
    keys: record.keys
  }
end
