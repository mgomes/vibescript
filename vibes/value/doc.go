// Package value defines the runtime Value type and its supporting
// domain types (Money, Duration, Range, time helpers) used throughout
// Vibescript. The vibes package re-exports these via type aliases so
// the embedder-facing surface stays unchanged during the carve-out.
package value
