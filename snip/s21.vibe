config = { mode: "fast" }
config[:missing]                      # => nil
config.fetch(:mode)                   # => "fast"
config.fetch(:missing, "default")     # => "default"
config.fetch(:missing) { |key| "no #{key}" }
