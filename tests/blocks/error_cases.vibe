def each_without_block
  [1, 2, 3].each
end

def map_without_block
  [1, 2, 3].map
end

def reduce_empty_without_init
  [].reduce do |acc, x|
    acc + x
  end
end

def reduce_empty_with_init
  [].reduce(10) do |acc, x|
    acc + x
  end
end
